import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  root: {
    MinWidth: 0,
    maxWidth: 250,
  },
  love: {
    height: '1em',
    width: '1em',
    margin: '0px 0.05em 0px 0.1em',
    verticalAlign: '-0.1em',
  },
});
